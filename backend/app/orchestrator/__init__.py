"""Cross-agent intelligence layer.

The orchestrator runs every domain agent's scan(), collects their signals,
draws cross-domain links, and composes the morning brief — the headline
insight a CXO sees on load.
"""
