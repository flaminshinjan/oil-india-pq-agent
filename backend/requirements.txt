fastapi>=0.115.6,<0.116
uvicorn[standard]==0.32.1
pydantic==2.10.6
python-multipart==0.0.20
python-dotenv==1.0.1

# LangGraph + LangChain
langgraph==0.2.60
langchain==0.3.13
langchain-core==0.3.28
langchain-anthropic==0.3.1
langchain-community==0.3.13

# Anthropic SDK — bumped to satisfy pipecat-ai (~=0.49); still in
# langchain-anthropic's accepted range (>=0.41,<1).
anthropic==0.49.0

# Vector store
chromadb==0.5.23

# Embeddings
sentence-transformers==3.3.1

# Document parsing
python-docx==1.1.2
openpyxl==3.1.5
pypdf==5.1.0
pdfplumber==0.11.4

# Utilities
tqdm==4.67.1

# Web search — Tavily provides a clean JSON API for ranked web results;
# used as a last-resort fallback by the chat agent when the OIL corpus
# doesn't have the answer.
tavily-python==0.5.0

# Voice — Pipecat pipeline (Deepgram STT → Anthropic LLM → Cartesia TTS),
# served over WebSocket via the FastAPI integration. The unpinned range
# lets pip pick a release whose pydantic requirement (>=2.10) lines up
# with langchain / fastapi.
pipecat-ai[deepgram,cartesia,anthropic,silero,websocket]==0.0.62
numpy==1.26.4    # required by silero VAD

# PDF report generation
reportlab==4.2.5
