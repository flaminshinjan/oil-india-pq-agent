app = "oil-india-pq-frontend"
primary_region = "bom"

[build]
  dockerfile = "Dockerfile"
  [build.args]
    BACKEND_URL = "https://oil-india-pq-backend.fly.dev"

[env]
  PORT = "8080"
  BACKEND_URL = "https://oil-india-pq-backend.fly.dev"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

  [http_service.concurrency]
    type = "requests"
    soft_limit = 50
    hard_limit = 100

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
